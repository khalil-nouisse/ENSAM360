# --- FastAPI Web Server & Data Models ---
fastapi
# Installs uvicorn and other standard dependencies needed to run the server
# Use this line instead of installing fastapi and uvicorn separately:
# fastapi[standard] 
# (However, for deployment, listing core packages like pydantic/uvicorn separately is safer)
pydantic
uvicorn

# --- LangChain & LangGraph Dependencies ---
langchain-core           # For HumanMessage, AIMessage, etc.
langgraph                # For creating and running the agent
langchain-google-genai   # For the ChatGoogleGenerativeAI LLM

# --- Utility & HTTP Clients ---
requests                 # Used by your Neo4jBackendClient to make HTTP calls
python-dotenv            # Used to load environment variables (.env file)

# command to run the server:

# uvicorn api:app --host 127.0.0.1 --port 8000